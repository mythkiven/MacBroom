<!-- 待办.md —— 当前未完成项。活的列表，随时增删勾选。 -->
# 待办

> 编号规则 `TODO-YYYYMMDD-NN`。完成后勾掉，并在 [更新日志.md](更新日志.md) 追加对应条目互相指回。
> 本批为 v1.1 规划（里程碑 M2），**现处于规划阶段，尚未动手实现**。

## P0 — 安全 / 信任

- [ ] **TODO-20260628-01 卸载残留关联精准化 + 删前可展开复核**
  - 痛点来源：Mole #856（误删 Apple Notes）、#801（删掉 Claude Code CLI 活动版本）、#808（删 Chrome Remote Desktop 注册）、PureMac #51（删 Claude web app 连带删 `~/.claude`）、MacSai #98（卸载「元宝」误关联微信）。
  - scope：`app_leftovers.py` 每一项标注「归属哪个 App / 为何被判定为残留」；UI 支持展开看完整路径明细、按大小排序；保持 `recommend=False`、永不自动勾选；强化 bundle-id 关联与白名单，降低误配。
  - 验收：①每个残留项可见「归属 App + 判定理由」；②可展开列出该项下实际文件/子目录；③对一组已知易误判样本（如开发者 CLI 点文件、共享容器）不再误报；④新增单测覆盖关联判定。
  - 备注：与定位强相关，完成后把这些 issue 编号回填进 README 的 Alternatives 证据表（见 TODO-20260628-08 关联）。

- [ ] **TODO-20260628-02 `macbroom doctor` 预检 + 完全磁盘访问权限引导**
  - 痛点来源：PureMac/Mole 数十条「完全磁盘访问没授权一脸懵 / 误报没权限」类 issue。
  - scope：新增 `macbroom doctor` 子命令（对标 Mole 的 doctor）：检测 Python 版本、完全磁盘访问是否到位、关键扫描路径可读性、端口可用性；不足时给出一步步指引（如何在「系统设置 → 隐私与安全性 → 完全磁盘访问」授权运行它的终端/解释器）。Web UI 启动时若检测到权限不足，顶部给非阻断提示。
  - 验收：①`macbroom doctor` 输出各项 ✓/✗ 与修复指引；②无完全磁盘访问时能明确判定并引导，而非静默跳过；③`--json` 输出便于脚本消费。

- [ ] **TODO-20260628-06 pip / PyPI 发布**
  - 现状：PyPI 尚无 `macbroom`（`/pypi/macbroom/json` 返回 404），CI 仅 `pip install -e .`，无发布流程；README 的 `pipx install macbroom` 当前会失败。
  - scope：①本地 `python -m build` 产出 wheel+sdist，`twine check` 通过；②加 GitHub Release workflow：打 tag（如 `v1.1.0`）→ build → 用 PyPI Trusted Publisher / OIDC 发布（**不放 token**）；③发布首版后验证 `pipx install macbroom && macbroom` 全链路可用。
  - 验收：①`pip install macbroom` / `pipx install macbroom` 能装上并启动；②发布流程可由打 tag 触发、可复现；③版本号在 `pyproject.toml` 与 CLI `--version` 一致。

## P1 — 省空间 / 体验

- [ ] **TODO-20260628-09 扩充 App 缓存覆盖：Telegram / Minecraft**
  - 痛点来源：mac-cleanup #84 等（Steam/Discord/Minecraft 等桌面应用缓存）。现已覆盖 Slack/Discord/VS Code/Teams/Spotify/Steam，缺 Telegram、Minecraft。
  - scope：在 `caches.py` 的 `_APP_CACHES` 补齐 Telegram、Minecraft 的**明确缓存子目录**（绝不碰账号/存档数据）；保持 `recommend=True`、`RISK_SAFE`。
  - 验收：①命中后只列缓存子目录，不含数据目录；②路径不存在时静默跳过；③体积按 `st_blocks` 计。

- [ ] **TODO-20260628-04 尊重 CACHEDIR.txt 标准**
  - 痛点来源：Mole #847。
  - scope：遍历/清理时识别目录内的 `CACHEDIR.txt` 标记——既可安全把标记目录视作缓存，也避免误动被标注「勿动」的目录；在 `fsutil` / 相关扫描器统一收口。
  - 验收：①含 `CACHEDIR.txt` 的标准缓存目录被正确识别；②单测覆盖标记存在/不存在两种情况。

- [ ] **TODO-20260628-05 结果按大小排序 + 分组 drill-down**
  - 痛点来源：Mole #1062/#852/#859、MacSai #85/#68（看清里面是什么、按大小排序、逐项勾选）。
  - scope：Web UI 增加按大小/数量排序；分组项可展开查看其下条目明细后再决定是否勾选。
  - 验收：①结果可切换排序；②分组可展开看明细；③选择状态在重扫后保留（沿用现有行为）。

## 收尾（carryover）

- [ ] **TODO-20260628-07 README 截图占位修复**
  - 现状：`docs/` 目录不存在；README 那两行图片标签写在 ```` ```markdown ```` 代码块里（占位示例），GitHub 不渲染。
  - scope：跑起 GUI 截真实主窗口图存 `docs/screenshot.png`，把占位替换成真正的 `![]()`；`docs/demo.gif` 需录屏（动图无法自动截，待补）。
  - 验收：①仓库主页 README 能显示真实主窗口截图；②demo.gif 要么补上、要么先移除该行不留破图。

- [ ] **TODO-20260628-08 README 亮点表扩充 + 安装说明与 pip 发布状态对齐（随 1.1 发版统一更新）**
  - 决策（2026-06-28）：1.1 新能力的亮点不提前写，**等 1.1 发版时统一更新 README**。
  - scope A（安装对齐）：发布前把 `pip install` 标注「即将支持」或下调主次，避免装失败；发布后恢复为主推。
  - scope B（补当前已成立但未写进表的优势 3 条）：
    - 体积显示准（`st_blocks` 算稀疏文件真实占用）→ 对 Pearcleaner 体积负数/不准、mac-cleanup human-bytes 错。
    - 中英双语母语质量（zh/en）→ 对 Pearcleaner #83 等未翻译/翻译错。
    - iOS 模拟器走 `xcrun simctl` 正规接口 → 对 mac-cleanup #69/#39 模拟器清理报错。
  - scope C（给现有 6 行挂真实 issue 证据号，evidence-backed）：卸载误关联行挂 Mole #856（删 Apple Notes）、#801（删 Claude CLI 活动版本）、MacSai #98（元宝误关联微信）等。
  - scope D（1.1 新能力亮点）：doctor 预检/权限引导（TODO-02）、CACHEDIR.txt（TODO-04）、排序+drill-down（TODO-05）、关联精准化（TODO-01）随发版补入亮点表。
  - 验收：README 安装路径与 PyPI 实际状态一致；亮点表覆盖全部已核实优势并尽量挂 issue 证据号。
