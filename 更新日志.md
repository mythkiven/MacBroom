<!-- 更新日志.md —— 时间线流水，只增不删，每次任务结束追加一条。 -->
# 更新日志

## 2026-06-28（续）— TODO-08 README 亮点表 + 安装说明

- 「Why MacBroom」表补 3 条已成立优势（`st_blocks` 体积准 / 中英母语质量 / `xcrun simctl` 模拟器）+ 新增「按名字误删 Apple Notes·Claude CLI」行（呼应 TODO-01 的 bundle-id 精确匹配 + 可展开复核）；中英同步。
- 功能列表补 1.1 能力：残留可解释、排序与展开、尊重 CACHEDIR.TAG、新增覆盖 Telegram/Minecraft。
- 安装说明在 PyPI 首发前改为「源码优先、pip 标注首发后可用」，避免用户 `pip install` 失败；首发后一行即可恢复 pip 主推。

## 2026-06-28（续）— v1.1 功能实现

- **TODO-01** 卸载残留：`is_installed_bundle` 精确匹配；每项 `reason` + 可展开 `children`；删前确认列路径/理由；跳过 Public/Printers。
- **TODO-02** `macbroom doctor`：Python/macOS/FDA/日志/端口五项检查 + 中英指引 + `--json`。
- **TODO-04** CACHEDIR：`fsutil.is_marked_cache_dir`（TAG + txt）；缓存扫描标注。
- **TODO-05** Web UI：按大小/名称排序；行内 drill-down；确认清理列明细。
- **TODO-09** 缓存：Telegram、Minecraft（logs/crash-reports/webcache）。
- **TODO-07** README：`docs/screenshot.png` 真实截图，去掉占位代码块。
- 单测 9→12 全过。

## 2026-06-28（续）— TODO-06 pip/PyPI 发布（进行中）

- 本地验证构建：`python -m build` 产出 wheel+sdist，`twine check` 两者 PASSED；新建干净 venv 安装 wheel 后 `macbroom`/`macbroom scan --json` 入口可用。
- 修复版本不一致：`macbroom/__init__.py` 的 `__version__` 由 `0.1.0` 同步为 `1.1.0`（与 `pyproject.toml` 对齐）。
- CLI 新增 `--version`：优先读已安装包元数据，源码运行回退 `__init__.__version__`；已安装与 `python -m macbroom` 两路均输出 `macbroom 1.1.0`。9 个单测全过。
- 新增 `.github/workflows/release.yml`：打 `v*` 标签 → 构建+twine 校验+校验标签与版本一致 → PyPI OIDC 可信发布（**不放 token**）→ 建 GitHub Release。
- 新增 `RELEASING.md`：PyPI Trusted Publisher 一次性配置 + 每次发版步骤 + 发布后验证。
- 待用户在 PyPI 侧登记 pending publisher 后，推 `v1.1.0` 标签即可首发（关联 TODO-20260628-06）。

## 2026-06-28

- 引入文档驱动开发三件套（`整体规划.md` / `待办.md` / `更新日志.md`），作为上下文窗口之外的长期记忆，防止迭代跑偏。
- 完成 v1.1 规划：通读 Mole / PureMac / MacSai 三个竞品的全部 issue（含已关闭）挖真实痛点，定下 M2 里程碑范围，并明确边界（不做可视化 treemap / 硬链接合并 / RAM 优化 / 主动卸载在装 App / Homebrew / 多语言扩充）。本轮仅出规划，未动功能代码。
- 复核一份外部痛点分析后校正计划：确认「排除清单 / 扫描可取消 / iCloud 打标 / 登录项 / CLI / 多数 App 缓存」均**已实现**（该分析为更早快照、判断过时）；据此①砍掉 TODO-03（.lproj 语言包剥离，高风险低收益，移入边界/M3）；②新增 TODO-20260628-09（补 Telegram/Minecraft 缓存）。当前 v1.1 待办：TODO-01/02/06（P0）、04/05/09（P1）、07/08（收尾）。
- 仓库 SEO：对齐 GitHub topics 与 `pyproject.toml` keywords——删冗余/自动识别项（`python`、`mac`），补竞品/场景关键词（`cleanmymac`、`appcleaner`、`pearcleaner`、`xcode-cleaner`、`homebrew`、`privacy`），最终 18 个；去掉与功能不符的 `daisydisk`。
- README：英文设为默认（全球 SEO），中文移至 `README.zh.md`；hero 区双语简介 + 证据化的「Why MacBroom」表。

### v1.0.0（已发布，tag `v1.0.0`）

- 首发：核心扫描器（缓存 / 卸载残留 / 大文件 / iOS 开发垃圾 / 重复文件 / 登录项 / 其它清理）+ 本地 Web UI + CLI。
- 安全模型：废纸篓兜底、受保护路径硬阻断、风险分级、危险项默认隐藏、删前 dry-run 确认、审计日志、iCloud 感知、可取消扫描、排除清单。
- 纯 Python 3 标准库，零第三方依赖。
